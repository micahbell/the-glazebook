extends ../layout

block content
  .container
    .row
      h1 Glazes Index
      if profile
        img(src=profile.pictureUrl)
        p #{profile.firstName}#{profile.lastName}
        p #{profile.id}
        p #{profile.emailAddress}
      a(href='/auth/logout')
        button Logout
        
      
